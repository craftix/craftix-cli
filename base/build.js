module.exports = function (craftix, config) {
    // If you want to apply things like Sass compilation, do it here,
    // but don't forget to change buildFolder to something like 'build' int the config
};